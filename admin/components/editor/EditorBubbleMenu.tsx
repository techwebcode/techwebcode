"use client";

import { Editor } from "@tiptap/react";

interface Props {
    editor: Editor;
}

export default function EditorBubbleMenu({
    editor,
}: Readonly<Props>) {
    if (!editor) return null;
    return null;
}